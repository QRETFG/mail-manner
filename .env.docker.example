AUTH_SECRET=replace-with-a-long-random-secret
# Use false for direct http://IP:3000 access. Set true only behind HTTPS.
AUTH_COOKIE_SECURE=false
# Optional: set this when your server cannot access Docker Hub directly.
NODE_IMAGE=mirror.gcr.io/library/node:22-bookworm-slim
